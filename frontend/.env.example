# Frontend environment variables
# Copy this file to frontend/.env for local/dev or set it in your host.

# Optional backend base URL (no trailing slash).
# Leave unset to let the app auto-try localhost:8000, :8001, and :8002 in dev.
# VITE_API_URL=http://localhost:8001
